# Production requirements - optimized for minimal Docker image
# Removed unused dependencies: numpy, sqlalchemy, alembic, aiohttp, 
# cryptography, pytest, python-telegram-bot, click, asyncio

# Core dependencies
requests==2.31.0
websocket-client==1.7.0
websockets==12.0
python-dotenv==1.0.0

# Data handling - No external dependencies needed (using plain Python)

# Display and UI
rich==13.7.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# Logging
loguru==0.7.2